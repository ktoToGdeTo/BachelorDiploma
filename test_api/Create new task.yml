info:
  name: Create new task
  type: http
  seq: 3

http:
  method: POST
  url: "{{BASE_URL}}/tasks"
  body:
    type: json
    data: |-
      {
        "title":"My first task22",
        "description":"The most interesting part of this task. Hihihaha",
        "status":"IN_PROGRESS"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
