info:
  name: Delete user (ADMIN ONLY)
  type: http
  seq: 13

http:
  method: DELETE
  url: "{{BASE_URL}}/users/delete/admin1"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
