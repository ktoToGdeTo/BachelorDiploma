info:
  name: Get task by id
  type: http
  seq: 1

http:
  method: GET
  url: "{{BASE_URL}}/tasks/2"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
