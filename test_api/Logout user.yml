info:
  name: Logout user
  type: http
  seq: 6

http:
  method: POST
  url: "{{BASE_URL}}/auth/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
