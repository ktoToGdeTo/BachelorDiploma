info:
  name: Delete task (ADMIN ONLY)
  type: http
  seq: 12

http:
  method: DELETE
  url: "{{BASE_URL}}/tasks/3"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
