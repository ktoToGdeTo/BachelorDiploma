info:
  name: Register User
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/users/register"
  body:
    type: json
    data: |-
      {
       "firstName":"Mike",
        "lastName":"Smith",
        "birthDate":"1987-03-02",
        "username":"mike1987",
        "password":"123"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
