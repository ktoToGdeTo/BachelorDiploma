info:
  name: Register User
  type: http
  seq: 4

http:
  method: POST
  url: "{{BASE_URL}}/users/register"
  body:
    type: json
    data: |-
      {
       "firstName":"George",
        "lastName":"Nolan2",
        "birthDate":"2022-02-01",
        "username":"admin",
        "password":"admin"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
