info:
  name: Login user
  type: http
  seq: 5

http:
  method: POST
  url: "{{BASE_URL}}/login"
  body:
    type: form-urlencoded
    data:
      - name: username
        value: mike1987
        disabled: true
      - name: password
        value: "123"
        disabled: true
      - name: username
        value: admin
      - name: password
        value: admin
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
