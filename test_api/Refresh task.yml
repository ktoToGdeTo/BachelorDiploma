info:
  name: Refresh task
  type: http
  seq: 11

http:
  method: PUT
  url: "{{BASE_URL}}/tasks/1"
  body:
    type: json
    data: |-
      {
        "title":"My first task",
        "description":"lol",
        "status":"CLOSED"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
