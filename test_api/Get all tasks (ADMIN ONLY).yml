info:
  name: Get all tasks (ADMIN ONLY)
  type: http
  seq: 9

http:
  method: GET
  url: "{{BASE_URL}}/tasks/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
