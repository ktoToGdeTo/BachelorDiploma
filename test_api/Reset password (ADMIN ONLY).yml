info:
  name: Reset password (ADMIN ONLY)
  type: http
  seq: 10

http:
  method: PUT
  url: "{{BASE_URL}}/admin/resetpassword"
  body:
    type: json
    data: |-
      {
        "username":"ryanGosling111",
        "password":"67"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
