info:
  name: Get all users (ADMIN ONLY)
  type: http
  seq: 7

http:
  method: GET
  url: "{{BASE_URL}}/users/all"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
