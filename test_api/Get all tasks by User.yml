info:
  name: Get all tasks by User
  type: http
  seq: 2

http:
  method: GET
  url: "{{BASE_URL}}/tasks"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
